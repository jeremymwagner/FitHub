<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="#3f86b0"

    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".Login"
    android:weightSum="1">



    <!-- Email text box for input-->
    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textEmailAddress"
        android:layout_marginTop="150dp"
        android:layout_centerHorizontal="true"
        android:ems="10"
        android:hint="@string/email"
        android:id="@+id/login_email" />

    <!-- Password text box for input-->
    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/login_password"
        android:hint="@string/password"
        android:layout_marginTop="15dp"
        android:inputType="textPassword"
        android:layout_below="@id/login_email"
        android:layout_alignLeft="@+id/login_email"
        android:layout_alignStart="@+id/login_email"
        android:layout_alignRight="@+id/login_email"
        android:layout_alignEnd="@+id/login_email" />

    <!--  Login button to verify credentials-->
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/login"
        android:id="@+id/login_button"
        android:textSize="15dp"
        android:layout_marginTop="50dp"
        android:layout_below="@+id/login_password"
        android:layout_centerHorizontal="true"
        android:layout_alignLeft="@+id/login_email"
        android:layout_alignStart="@+id/login_email"
        android:layout_alignRight="@+id/login_email"
        android:layout_alignEnd="@+id/login_email"/>


    <!-- Google sign in button for use of Google API-->
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="LOGIN WITH GOOGLE"
        android:background="@color/fb_button"
        android:textColor="#FFFFFF"
        android:layout_marginTop="15dp"
        android:id="@+id/fb_button"
        android:layout_below="@+id/login_or"
        android:layout_centerHorizontal="true"
        android:layout_alignLeft="@+id/login_email"
        android:layout_alignStart="@+id/login_email"
        android:layout_alignRight="@+id/login_email"
        android:layout_alignEnd="@+id/login_email"/>


    <!-- Sign up button to register new account-->
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/signup"
        android:textSize="15dp"
        android:id="@+id/login_signup"
        android:layout_below="@+id/login_button"
        android:layout_marginTop="15dp"
        android:layout_alignLeft="@+id/fb_button"
        android:layout_alignStart="@+id/fb_button"
        android:layout_alignRight="@+id/fb_button"
        android:layout_alignEnd="@+id/fb_button" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="15dp"
        android:text="OR"
        android:textStyle="bold"
        android:id="@+id/login_or"
        android:layout_below="@id/login_signup"
    />



    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="FitHub"
        android:id="@+id/login_title"
    
        android:layout_centerHorizontal="true"
        android:layout_marginTop="53dp" />
</RelativeLayout>
